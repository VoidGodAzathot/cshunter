{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Capability for the preload window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
